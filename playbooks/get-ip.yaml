- name: 📡 ดึง Public IP ของ VM
  hosts: localhost
  connection: local
  gather_facts: no

  vars_files:
    - ../config/config-dev.yaml

  tasks:
    - name: 📥 ดึงข้อมูล Public IP จาก Azure
      azure.azcollection.azure_rm_publicipaddress_info:
        resource_group: "{{ resource_group }}"
        name: "{{ pip_name }}"
        auth_source: env
      register: ip_info

    - name: 🐛 DEBUG ข้อมูล IP ที่ได้
      debug:
        var: ip_info

    - name: ❌ Fail ถ้าไม่พบ IP
      fail:
        msg: "ไม่พบ Public IP ของ VM"
      when: ip_info.publicipaddresses is not defined or ip_info.publicipaddresses | length == 0

    - name: 🧹 ลบไฟล์ ip.txt เก่าถ้ามี
      file:
        path: "/home/boho/ip.txt"
        state: absent
        force: yes

    - name: 📝 เขียน Public IP ลงไฟล์ใหม่
      copy:
        content: "{{ ip_info.publicipaddresses[0].ip_address }}"
        dest: "/home/boho/ip.txt"
